# Sprite Font Editor 用户指南

## 目录

1. [快速开始](#快速开始)
2. [界面介绍](#界面介绍)
3. [功能详解](#功能详解)
4. [高级用法](#高级用法)
5. [常见问题](#常见问题)
6. [快捷键](#快捷键)

---

## 快速开始

### 第一步：上传图片

1. 点击工具栏的"上传图片"按钮
2. 选择支持的图片格式 (PNG、JPG、GIF、WebP)
3. 图片将自动适配编辑区域

### 第二步：上传字体

1. 点击工具栏的"上传字体"按钮
2. 选择字体文件 (TTF、OTF、WOFF、WOFF2)
3. 等待字体加载完成（会显示成功提示）

### 第三步：配置网格

1. 在右侧面板中找到"网格配置"
2. 设置单元格宽度和高度
3. 调整单元格间距和边距
4. 启用/禁用网格显示

### 第四步：输入文字

1. 在右侧面板的"字符输入"区域
2. 输入要渲染的文字（如：Hello World）
3. 系统会自动将每个字符分配到单元格

### 第五步：导出图片

1. 点击工具栏的"导出PNG"按钮
2. 图片将自动下载到本地
3. 文件名包含时间戳，如 `sprite-font-2025-12-05-12-00-00.png`

---

## 界面介绍

### 工具栏（顶部）

| 按钮 | 功能 |
|------|------|
| 上传图片 | 上传底图 |
| 上传字体 | 上传字体文件 |
| 渲染文字 | 手动触发文字渲染 |
| 导出PNG | 导出最终结果 |
| 清空 | 清空所有内容 |

**插入点模式**：
- **自动**：自动检测透明区域并定位插入点
- **手动**：手动点击选择插入位置

### 画布区域（中央）

- **底层 Canvas**：显示底图和渲染的文字
- **上层 UI 层**：显示网格、高亮等交互元素

### 右侧面板

#### 图片配置
- **外边距**：图片周围的外边距
- **内边距**：图片内部的内边距

#### 网格配置
- **单元格宽度**：单个单元格的宽度
- **单元格高度**：单个单元格的高度
- **单元格间距**：单元格之间的间距
- **显示选项**：网格线、边距线、填充线

#### 字符样式
- **字体**：字体族名称
- **字号**：字符大小
- **颜色**：字符颜色
- **描边**：描边颜色、宽度、开关

#### 插入点信息
- 显示当前插入点坐标
- 显示可用/已用/总单元格数

#### 字符输入
- 文本输入框
- 字符列表显示
- 批量删除选项

---

## 功能详解

### 插入点自动检测

**工作原理**：
1. 分析图片的透明区域
2. 识别可以放置文字的空白单元格
3. 从第一个可用单元格开始填充

**使用场景**：
- 图片有预先留好的空白区域
- 需要批量填充多个字符
- 快速定位可用位置

### 手动选择模式

**如何使用**：
1. 切换到"手动"模式
2. 在画布上点击任意单元格
3. 该单元格会显示红色高亮
4. 文字将从该位置开始填充

**使用场景**：
- 精确控制插入位置
- 跳过某些单元格
- 自定义填充顺序

### 字符对齐

**水平对齐**：
- **左对齐**：字符靠左
- **居中**：字符居中
- **右对齐**：字符靠右

**垂直对齐**：
- **顶部**：字符靠上
- **居中**：字符垂直居中
- **底部**：字符靠下

### 描边效果

**用途**：让文字在不同背景的单元格中更清晰

**设置方法**：
1. 启用"描边"开关
2. 选择描边颜色（通常为白色）
3. 调整描边宽度（1-3px）

**最佳实践**：
- 浅色背景：黑色文字 + 白色描边
- 深色背景：白色文字 + 黑色描边

---

## 高级用法

### 批量处理

1. **准备字符列表**：在字符输入框中输入所有需要的字符，如 `ABCDEFGHIJKLMNOPQRSTUVWXYZ`
2. **系统自动分配**：每个字符会被自动分配到连续的单元格
3. **检查排列**：确保所有字符都在正确的位置

### 字符间距调整

1. **单元格间距**：调整网格配置中的间距
2. **字符边距**：为特定字符设置边距（高级功能）

### 透明度检测优化

**提高检测精度**：
1. 在图片配置中减少内边距
2. 确保图片背景是纯透明的
3. 避免使用半透明区域

### 性能优化技巧

**处理大图片**：
- 图片尺寸建议不超过 1000x1000 像素
- 如需处理更大图片，考虑预先压缩

**处理大量字符**：
- 分批测试，避免一次性加载过多
- 使用合适的单元格大小

---

## 常见问题

### Q: 为什么字体上传后没有效果？

**A**: 可能原因：
1. 字体文件损坏 - 尝试其他字体文件
2. 字体格式不支持 - 使用 TTF、OTF、WOFF 格式
3. 浏览器缓存 - 刷新页面重试

### Q: 为什么有些字符显示不全？

**A**: 可能原因：
1. 单元格太小 - 增大单元格尺寸
2. 字符边距设置不当 - 调整边距设置
3. 描边太宽 - 减小描边宽度

### Q: 导出图片的质量如何？

**A**: 导出图片质量取决于：
1. 原始图片的分辨率
2. Canvas 的尺寸设置
3. 使用的字体大小

### Q: 支持哪些图片格式？

**A**: 支持的图片格式：
- PNG（推荐，支持透明）
- JPG/JPEG
- GIF
- WebP

### Q: 可以在移动设备上使用吗？

**A**: 可以，但注意：
- 确保屏幕足够大
- 文件选择可能受限
- 建议在大屏幕上操作

### Q: 状态会自动保存吗？

**A**: 是的，系统会自动：
- 保存所有配置到 localStorage
- 刷新页面后恢复状态
- 清除浏览器数据后会丢失

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + O` | 上传图片 |
| `Ctrl + F` | 上传字体 |
| `Ctrl + S` | 导出PNG |
| `Ctrl + R` | 重新渲染 |
| `Delete` | 清空画布 |
| `Esc` | 取消选择 |

**注意**：快捷键功能需要在组件中实现

---

## 最佳实践

### 图片准备

1. **尺寸合适**：根据目标平台确定尺寸
2. **透明区域**：为文字预留透明区域
3. **格式选择**：使用 PNG 保持透明

### 字体选择

1. **清晰易读**：选择清晰的字体
2. **文件大小**：避免过大的字体文件
3. **版权注意**：确保有使用权

### 工作效率

1. **先测试**：用小图和小数据测试
2. **批量处理**：确认无误后再批量操作
3. **保存配置**：利用自动保存功能

---

## 故障排除

### 画布不显示

1. 检查是否上传了图片
2. 刷新浏览器重试
3. 检查浏览器控制台是否有错误

### 文字不渲染

1. 确认已上传字体
2. 检查字符输入框是否有内容
3. 确认插入点位置正确

### 导出失败

1. 确认浏览器允许下载
2. 检查 Canvas 是否正常显示
3. 尝试使用其他浏览器

---

## 技术支持

如遇到无法解决的问题，请：

1. 查看浏览器控制台错误信息
2. 导出并保存错误信息
3. 查看项目文档和示例

---

**文档版本**: 1.0  
**最后更新**: 2025-12-05
